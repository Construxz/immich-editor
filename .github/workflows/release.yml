name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 25
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.47.5
          channel: stable
          cache: true

      - name: Signierschlüssel aus den Secrets
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          test -n "$KEYSTORE_BASE64" || { echo "Secret ANDROID_KEYSTORE_BASE64 fehlt"; exit 1; }
          echo "$KEYSTORE_BASE64" | base64 -d > android/app/upload.jks
          cat > android/key.properties <<PROPS
          storeFile=upload.jks
          storePassword=$KEYSTORE_PASSWORD
          keyAlias=$KEY_ALIAS
          keyPassword=$KEY_PASSWORD
          PROPS

      - run: flutter pub get
      - name: APKs bauen
        # Version und Build-Nummer aus pubspec.yaml (D-82); der Tag muss dazu passen.
        run: |
          V=$(sed -n 's/^version: *//p' pubspec.yaml)
          test "v${V%%+*}" = "$GITHUB_REF_NAME" || { echo "Tag $GITHUB_REF_NAME passt nicht zu pubspec $V"; exit 1; }
          flutter build apk --release --split-per-abi
          flutter build apk --release

      - name: Umbenennen und Prüfsummen
        run: |
          mkdir dist
          cd build/app/outputs/flutter-apk
          for f in app-*release.apk; do
            abi=${f#app-}; abi=${abi%release.apk}; abi=${abi%-}
            cp "$f" "../../../../dist/immich-editor-$GITHUB_REF_NAME-${abi:-universal}.apk"
          done
          cd ../../../../dist && sha256sum *.apk > SHA256SUMS.txt && cat SHA256SUMS.txt

      - name: GitHub-Release
        env:
          GH_TOKEN: ${{ github.token }}
        # Mit Bindestrich (0.1.0-rc.1, 0.1.0-dev.N) eine Vorabversion.
        run: |
          case "$GITHUB_REF_NAME" in *-*) PRE=--prerelease ;; *) PRE= ;; esac
          gh release create "$GITHUB_REF_NAME" dist/* --title "$GITHUB_REF_NAME" --generate-notes $PRE
